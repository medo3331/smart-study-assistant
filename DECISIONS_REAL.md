# DECISIONS.md — الحقيقة (بعد Reality Sync، أبريل 2025)

هذا الملف يحل محل DECISIONS.md النظري السابق. كل القرارات هنا مبنية على الواقع المُحقَّق من smart-study-assistant، مش على افتراضات.

## قاعدة ذهبية جديدة (بعد Reality Sync)
كل قرار جديد يجب أن يُراجع ضد الـ 5 ملفات الحقيقية:
CURRENT_STATE_REAL.md → ARCHITECTURE_REAL.md → EDUCATION_SYSTEM_REAL.md → AI_SYSTEM_REAL.md → DATABASE_REAL.md
إذا تعارض مع أي واحد منهم → يُرفض أو يُعدّل أولًا.

---

## D-REAL-001 — لا نعيد بناء من الصفر
قرار: Magiclly = منتج حقيقي (`smart-study-assistant`). نطوّره ونكمله، مش نبدأ نظام جديد. كل الملفات النظرية السابقة تُعتبر "مرجع تاريخي" مش مصدر حقيقي.
حالة: مقفل. لا يُفتح بدون مراجعة الـ 5 ملفات.

## D-REAL-002 — 6 مراحل حقيقية، مش 3 نظرية
قرار: Primary / Preparatory / Secondary / University / Graduate / Freelancer — كلها موجودة في DB والتنقل. Secondary = الأولوية الفعلية حاليًا.
حالة: مقفل.

## D-REAL-003 — Secondary أولًا، مش ميزات جديدة لمراحل مبكرة
قرار: قبل أي تطوير لـ Graduate أو Freelancer أو ميزات "مبهرة"، نكمل Secondary ونثبّت المراحل الأقوى (Primary/Preparatory).
حالة: مقفل.

## D-REAL-004 — AI Router موجود، Tutor OS هدف مستقبلي
قرار: الـ AI الحالي = Router + نماذج متعددة. Tutor OS (5 subsystems متكاملة) مش موجود كوحدة. أي تطوير AI يبدأ من Router ويطوّره تدريجيًا، مش يعيد بناؤه من الصفر.
حالة: مقفل.

## D-REAL-005 — Hermes = أداة الشركة فقط
قرار: لا نعيد إدخال أي نظام تنفيذ موازي. لا ZCode (متوقف نهائيًا). الطالب يستخدم تطبيق الويب المنفصل. CTO يعمل داخل Hermes فقط.
حالة: مقفل.

## D-REAL-006 — DB الحقيقية (Supabase) هي المصدر
قرار: لا نعيد تصميم DB بناءً على schema نظري. أي إضافة جداول (مثل Evidence أو ConceptNode) يجب أن تكون مبررة بـ حاجة فعلية من الـ Router أو Secondary، مش "لأنها في التصميم النظري".
حالة: مقفل.

## D-REAL-007 — لا ميزات قبل تصحيح المعرفة
قرار: P0 = تصحيح المعرفة (Reality Sync الكامل). لا نبدأ ميزات جديدة أو تصميم Tutor OS قبل إكمال مراجعة الـ 5 ملفات الحقيقية.
حالة: مقفل — جارٍ حاليًا.

## D-REAL-008 — كل ما لم يُتحقق = UNKNOWN
قرار: إذا لم يُوجد دليل من الكود أو DB أو .env، نُسجّل UNKNOWN — لا نفترض. هذا يحمي من بناء ميزات على وهم.
حالة: مقفل.

## D-REAL-009 — اللغة والتواصل
قرار: من الآن فصاعدًا، كل النقاش الاستراتيجي والتقني مع CTO يكون بالعربية المصرية البسيطة. الملفات تُكتب بالعربية أو الإنجليزية حسب السياق، لكن التواصل المباشر = عربي.
حالة: مقفل.

## D-REAL-010 — الملفات النظرية السابقة = مرجع تاريخي فقط
قرار: VISION.md / CURRENT_STATE.md / ARCHITECTURE.md / EDUCATION_SYSTEM.md / AI_SYSTEM.md / DATABASE.md / DECISIONS.md (النسخ النظرية) لا تُستخدم كمصدر حقيقي بعد الآن. النسخ الحقيقية (مع _REAL) هي المصدر.
حالة: مقفل.

---

## ما هو مفتوح (لتقرير مستقبلي، ليس الآن)
- كيف نربط AI Router بـ Tutor OS تدريجيًا (بعد مراجعة الكود الكامل).
- هل نحتاج graph (ConceptNode) حقيقي في DB أم نعمل مع دروس/خطط حالية.
- متى نبدأ v0 Primary Math Graph (متوقف مؤقتًا — لا يبدأ قبل إكمال Secondary ومراجعة الكود).
- نموذج العمل / المستخدمين الأوائل — لا قرارات تجارية قبل بيانات حقيقية.

## استخدام هذا الملف
قبل أي اقتراح ميزة أو تغيير كود أو قرار استراتيجي:
1. اقرأ الـ 5 ملفات الحقيقية أولًا.
2. راجع القرارات المقفلة هنا.
3. إذا تعارض → ارفع التناقض للـ CTO (أنا) مع دليل من الكود.
4. إذا لم يتعارض ويفيد Secondary → يمكن التنفيذ داخل Hermes.
