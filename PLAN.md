# خطة التطوير — من مذاكرة عامة لمنصة برمجة

> اتكتبت بعد فحص الكود الفعلي — كل رقم وكل اسم عمود هنا اتأكد منه.
>
> **حالة التنفيذ (٣٠ يوليو ٢٠٢٦):** القسم **سابعاً (طبقة الشخصية)** اتنفّذ بالكود —
> `lib/user-persona.ts` + `components/PersonaPicker.tsx` + توحيد `getUiText` + تدفق
> التسجيل بـ `?next=`. الناقص فيه: **الـ SQL على `profiles` لسه ما اتشغّلش** (سطر ٢٨١–٢٨٦).
> باقي الأقسام لسه خطة على ورق.
>
> **✅ الباگ اتصلح واتأكد ببناء أخضر (٣٠ يوليو):** `?next=` كان كسر الـ build —
> `useSearchParams()` في `/login` بيمنع توليد الصفحة static، و Next 16.2.10
> **بيرمي** خطأ وقت الـ export بدل ما يتساهل (لأن `cacheComponents` مقفول).
> **محاولة `<Suspense>` فشلت تلات مرات** (٠٣:٢٦، ٠٤:٢٢، ٠٧:٥٦) — **متتعادش**.
> **الحل اللي نفع:** الهوك **اتشال خالص**، و`next` بقى بيتقرا وقت الضغط من
> `window.location.search` في `resolveNext()` — مفيش هوك يبقى مفيش bail-out.
> الديبلويمنت شغّال على `smart-study-assistant-psi.vercel.app`، وبما إن فشل
> الـ export على `/login` كان بيوقّف الـ build كله (`exiting the build`)، فنجاح
> الديبلوي **دليل إن `/login` اتولّدت**. الباقي تحقق وقت التشغيل بس (الضغط والتوجيه).
> **الدرس:** `tsc` و `grep` و `npm run dev` **مش بيشوفوا** أخطاء وقت التوليد —
> اللي بيكشفها `next build` بس، وأنا مش بقدر أشغّله.

---

## أولاً: تلات تصحيحات قبل أي كود

### ١) «Variables / Loops / Functions» دي مواضيع أيام، مش مراحل درس

الليستة اللي بعتها بتخلط بين حاجتين. `study_days` فيه عمود `topic`، والخطة بتتولّد
كـ ١٤ يوم كل يوم ليه موضوع. يعني في تراك برمجة، **الأيام** هي اللي هتبقى
Variables و Loops و Functions. فلو خلّينا دول مراحل جوه الدرس الواحد، الدرس اللي
موضوعه أصلاً «Loops» هيبقى فيه مرحلة اسمها «Variables» — وده مالوش معنى.

اللي بعتّه فعلاً بند صالح كمرحلة هو ١ و ٥ و ٦ (Introduction / Quiz / Project).
الاقتراح إننا نكمّل الستة بمراحل شغّالة على **أي** درس برمجة:

| # | المرحلة | إيه اللي فيها |
|---|---------|---------------|
| ١ | مقدمة | الفكرة في ٣ سطور + ليه محتاجها |
| ٢ | شرح | المفهوم بالتفصيل + كود مرجعي |
| ٣ | تجربة | Playground — عدّل الكود وشوف الناتج |
| ٤ | تدريب | سؤال سريع واحد (Practice) |
| ٥ | اختبار | الكويز الموجود حالياً |
| ٦ | مشروع | تطبيق صغير يجمّع اللي فات |

وبكده بيبقى عندنا **سكّتين**: سكة الخطة فوق (اليوم ٣ من ١٤ — موجودة أصلاً)،
وسكة المراحل جوه الدرس. الأولى بتقول «إنت فين في الكورس»، والتانية «إنت فين في الدرس».

### ٢) الماركداون الحالي مش بيعرف بلوكات الكود — ده أهم باگ

`renderMarkdownLite` في `page.tsx` سطر ٥٩ بيتعامل مع `**عريض**` والقوائم و `##` بس.
مفيش أي معالجة للـ ` ``` `. يعني لو الـ AI رجّع:

    ```js
    for (let i = 0; i < 5; i++) {}
    ```

هيتعرض كنص عادي والباك-تيكس ظاهرة. ده مقبول في منصة أحياء، **بس في منصة برمجة ده كسر
كامل** — أهم حاجة في الشرح هي الكود. فده لازم يتصلح في المرحلة الأولى مش يتأجل.

### ٣) الـ Playground محتاج قرار أمان مش بس إيديتور

تشغيل كود المستخدم في نفس الصفحة (`eval`) معناه إن أي كود يقدر يقرا التوكن بتاع
Supabase من الـ storage. الطريقة الصح: `<iframe sandbox="allow-scripts">` منفصل +
`postMessage`، مع **مؤقّت قطع** لأن `while(true)` هيجمّد التاب. دي مش تفصيلة —
دي أساس التصميم، ولازم تتبني كده من أول سطر.

---

## ثانياً: تقسيم صفحة الدرس

الملف دلوقتي `app/lesson/[dayId]/page.tsx` = **١٠٤٥ سطر**. الأقسام الموجودة فيه:

- تحميل البيانات + **٢٦ متغير حالة** في كمبوننت واحد (سطر ٢١٩–٢٦١)
- إجراءات سريعة (حفظ / مفضلة / مشاركة)
- اختيار نمط التعلم
- «الشرح» — `SmartContentViewer` بأربع تابات
- «سؤال وجواب» — الشات
- «مراجع» — مستودع الروابط
- «اختبار» — الكويز
- مودال الاحتفال

التقسيم المقترح، بنفس نمط `app/dashboard/components/` اللي شغّال كويس:

```
app/lesson/[dayId]/
  page.tsx              ← تحميل + حالة + تخطيط بس        (~٢٥٠ سطر)
  components/
    types.ts            ← DayRow, ConfigRow, QuizQuestion, Stage
    markdown.tsx        ← renderMarkdownLite + Reveal + بلوكات الكود 🆕
    useLesson.ts        ← هوك التحميل والتقدم والدرس التالي
    LessonHeader.tsx    ← التنقل + مسطرة تقدم الخطة
    LessonHero.tsx      ← الختم + العنوان + ابدأ الدرس
    StageTimeline.tsx   ← 🆕 سكة المراحل
    ExplainPanel.tsx    ← SmartContentViewer الحالي
    LessonChat.tsx      ← الشات
    ResourceVault.tsx   ← المراجع
    QuizPanel.tsx       ← الاختبار
    CelebrationModal.tsx
```

**ملحوظة مهمة:** ده مش نقل كود وخلاص. التايم لاين بيغيّر الصفحة من «أقسام تحت بعضها»
لـ «مرحلة واحدة في المرة». يعني الأقسام الموجودة بتتحول من *سكشنز* لـ *محتوى مرحلة*.
ده أكبر جزء من الشغل، مش التقسيم نفسه.

---

## ثالثاً: قاعدة البيانات

الخبر الحلو: **مفيش جدول جديد ولا RLS جديدة**. كل حاجة عمود على `study_days`،
واللي عليه RLS شغّالة بالفعل. وفيه سابقة: `resource_links` أصلاً `jsonb`.

```sql
alter table study_days
  add column if not exists stages          jsonb   default '[]'::jsonb,
  add column if not exists current_stage   int     default 0,
  add column if not exists notes           text,
  add column if not exists summary         text,
  add column if not exists flashcards      jsonb   default '[]'::jsonb,
  add column if not exists mindmap         jsonb,
  add column if not exists playground_code text;
```

**ليه بنخزّن الملخص والفلاش كاردز في الداتابيز؟** لأنهم بيتولّدوا بالـ AI. من غير
تخزين، كل مرة المستخدم يفتح الدرس بندفع توكنز تاني وبيستنى تاني ونديله محتوى مختلف عن
اللي قراه إمبارح. مع التخزين: توليد واحد لكل درس.

و `current_stage` هو نفسه اللي هيشغّل **Continue Learning** بعدين — من غير أي شغل زيادة.

⚠️ أنا مش بقدر أشغّل الـ SQL ده. هتاخده وتحطّه في SQL Editor في Supabase بنفسك.

---

## رابعاً: أثر «مبرمجين بس» على باقي التطبيق

القرار ده بيلمس ملفات برّه صفحة الدرس. مش كلها لازم دلوقتي، بس لازم تكون معروفة:

| المكان | اللي بيفترضه دلوقتي | اللي لازم يبقى |
|--------|---------------------|----------------|
| `CategoryType` (٥ ملفات) | أكاديمية / مهارة / عمل / تطوير شخصي | تراك: لغة، فريم-وورك، أساسيات، مقابلات |
| `api/generate-plan` | «أنشئ خطة دراسية لمادة X» | «تراك برمجة بلغة X لمستوى Y» + مواضيع كود |
| `api/chat` البرومبتات | «أستاذ جامعي» + معادلات | «مهندس سينيور» + بلوكات كود إجبارية |
| `getSuggestedResources` سطر ٣٦٢ | Google Scholar، ملخصات PDF | MDN، التوثيق الرسمي، GitHub |
| كوبي اللاندينج | مذاكرة عامة | تعلّم البرمجة |

---

## خامساً: الترتيب

### مرحلة ١ — الأساس (ابدأ هنا)
تقسيم الصفحة لمكونات · بلوكات الكود في الماركداون · جدول المراحل والـ SQL ·
`StageTimeline` بالأخضر للمكتمل · ربط الكويز الموجود كمرحلة ٥.
**النتيجة:** الدرس بقى مراحل، والكود بيتعرض صح.

### مرحلة ٢ — جوه الدرس (سريعة، قيمتها عالية)
شيبس اقتراحات الـ AI فوق الشات (اشرح / اختبرني / ملخص / فلاش كاردز / مثال) ·
زرار Generate Summary · Practice بعد كل مرحلة · الملاحظات.
كلها بتستخدم `/api/chat` الموجود وأعمدة المرحلة ١. **أصغر شغل وأكبر إحساس بالفرق.**

### مرحلة ٣ — الـ Playground
CodeMirror 6 + iframe معزول + مؤقّت قطع. JavaScript الأول لأنها بتشتغل في المتصفح
ببلاش. لغات تانية = Pyodide (١٠ ميجا) أو API خارجي بفلوس — قرار منفصل وقته يجي.
**دي وحدها أكبر من المرحلتين ١ و ٢ مجمّعين.**

### مرحلة ٤ — المراجعة
فلاش كاردز جوه الدرس · Mind Map في الآخر.
للماين ماب: **SVG مرسوم بإيدينا** أحسن من مكتبة جاهزة — التصميم كله «ملزمة» مرسومة
بالإيد، وأي مكتبة هتحط شكل شركاتي غريب على الصفحة.

### مرحلة ٥ — حوالين الدرس
Continue Learning (شبه مجاني بعد المرحلة ١) · هدف يومي · كالندر · PDF
(`html2pdf.js` متسطّبة ومش مستخدمة) · `manifest.json` للـ PWA (الـ `sw.js` موجود
والناقص المانيفست بس).

### مرحلة ٦ — تحت علامة استفهام
**Voice AI** — بصراحة: التعرف على الكلام العربي في المتصفح ضعيف، وأصوات القراءة
العربية وحشة على أغلب الأجهزة. غالباً هيطلع demo مش feature. أنصح نأجّلها.
**Learning Path** — دي موجودة فعلياً؛ الخطة نفسها هي المسار. محتاج توضّح إنت قاصد إيه.
**Study Calendar** — بتتقاطع مع الـ Heatmap الموجود. نبني عليه بدل ما نعمل حاجة تانية.

---

## سادساً: اللي موجود بالفعل — بلاش نعمله تاني

| البند | الحالة |
|-------|--------|
| Heatmap | ✅ كامل — ٧٠ يوم في `AnalyticsSection` |
| Analytics أسبوعية | ✅ كامل — أسبوعي/شهري + مودال ملخص |
| Badges | ✅ جدول `badges` + `BossFight` |
| تذكير بالمذاكرة | ✅ web-push + `sw.js` + وقت في الإعدادات |
| Flashcards | 🟡 على الداشبورد بس — **مش** جوه الدرس |
| Focus | 🟡 تسجيل دقائق موجود، مفيش شاشة تركيز |
| PWA | 🟡 `sw.js` موجود، **مفيش** `manifest.json` |
| PDF | 🔴 المكتبة متسطّبة ومش مستوردة في أي ملف |
| Daily Reward | 🔴 اللي موجود `xp_reward` للدرس، مش هدية يومية |

---

## سابعاً: اختيار الشخصية (طالب / خريج / شغال حر)

### ١) ده محور تاني مش نفس المحور

«إنت مين» و «بتتعلم إيه» سؤالين مختلفين ولازم يفضلوا مختلفين. المشكلة في
`CategoryType` الحالي إنه بيخلط الاتنين في ليستة واحدة: «دراسة أكاديمية» ده شخصية،
«تعلم مهارة» ده هدف، و «عمل ومشاريع» ده سياق. فالمستخدم بيختار من ليستة بنودها
مش من نفس النوع.

التقسيم الصح:

- **الشخصية** (طالب / خريج / شغال حر) → بتحدد **ليه** بتتعلم: الهدف، النبرة، الوقت، المخرجات
- **التراك** (JS / React / أساسيات / مقابلات) → بيحدد **إيه** اللي بتتعلمه

وبكده «مبرمجين بس» و «مش نخلي الموقع متخصص في حاجة واحدة» مش متعارضين:
المحتوى برمجة، والاستخدام تلات استخدامات. طالب حاسبات بيذاكر مادة، وخريج بيبني
بورتفوليو ويستعد لمقابلة، وفري لانسر عنده تاسك عميل — تلاتتهم مبرمجين ومحتاجين
حاجات مختلفة تماماً.

### ٢) الخبر الحلو: الماكينة دي موجودة

`getUiText(category)` بيغيّر ٦ نصوص في الداشبورد حسب الاختيار (اسم القسم، بادئة
الخطوة، زرار الطوارئ، عنوان النقاط، زرار ماجيك، وصف المهمة). يعني «الواجهة تبقى
مختلفة لكل اختيار» مبنية أصلاً — إحنا بنغيّر المفتاح اللي بتشتغل بيه بس.

⚠️ بس هي **متكرّرة ٣ مرات**: `theme-helpers.ts` (نسخة مصدّرة)، و `dashboard/page.tsx`
(نسخة inline جوه الكمبوننت)، و `getStepPrefix` تالتة في `assessment/page.tsx`.
لازم تتوحّد في ملف واحد وقت التحويل، مش تتنسخ رابع مرة.

### ٣) اللي بيختلف فعلاً لكل شخصية

لو الفرق مش محدّد بالضبط، الاختيار هيبقى مجرد لافتة. الفرق المقترح:

| | 🎓 طالب | 💼 خريج | 🧑‍💻 شغال حر |
|---|---|---|---|
| الهدف | يفهم المنهج وينجح | بورتفوليو + مقابلات | يخلّص تاسك عميل |
| وحدة التقدم | «الدرس ٣» | «المشروع ٢» | «التاسك» |
| مرحلة ٥ (اختبار) | كويز | سؤال مقابلة | مراجعة كود |
| مرحلة ٦ (مشروع) | تمرين صغير | قطعة بورتفوليو | حاجة تنفع تتسلّم |
| الموارد | توثيق + شرح | LeetCode + أسئلة مقابلات | GitHub + حلول جاهزة |
| نبرة الـ AI | معلّم | مدرّب مقابلات | زميل سينيور |
| الداشبورد | ستريك و XP | جاهزية المقابلة | ديدلاين وتسليم |

⚠️ **قاعدة صيانة:** الشخصية تأثيرها يتوقف عند تلات حاجات — نصوص، سطر واحد يتحقن في
برومبت الـ AI، وليستة الموارد. **ممنوع مسارات كود منفصلة لكل شخصية**، وإلا كل باگ
هنصلحه تلات مرات وكل فيتشر هنبنيه تلات مرات.

### ٤) فين يتحط الاختيار

الهيرو دلوقتي فيه إجراء أساسي أصفر واحد. لو حطينا ٣ كروت كبيرة جواه، ميزانية اللون
تتكسر والصفحة تبقى مفيهاش نقطة تركيز. المقترح:

- **سكشن مستقل تحت الهيرو** فيه التلاتة، الاختيار بالحبر (`bg-ink text-paper-2`)
  مش بالأصفر
- الاختيار يتخزن في `localStorage` (لسه مفيش يوزر)، وكوبي اللاندينج نفسه يتغير
  بعده — الخطوات والميزات تتكلم بلغة الشخصية المختارة. **ده اللي بيخلي الزائر يحس
  إن الموقع بقى بتاعه قبل ما يسجّل حتى.**
- في `/assessment` الاختيار يبقى جاهز مختار، ويتكتب في الداتابيز

**التدفق المطلوب — ٤ خطوات، والتسجيل آخر حاجة:**

| # | الخطوة | تظهر لمين |
|---|--------|-----------|
| ١ | الشخصية — طالب / خريج / شغال حر | الكل |
| ٢ | المستوى — إعدادي / ثانوي / جامعة / ماستر | **لو اختار طالب بس** |
| ٣ | المادة أو التراك اللي عايز خطة فيه | الكل |
| ٤ | تسجيل الدخول | الكل — **بعد** الاختيار |

الخطوة ٢ بتتفتح جوه نفس السكشن لما يختار «طالب»، ولو اختار خريج أو شغال حر
بتتخطّى تماماً. الزرار الأصفر في السكشن مبيشتغل غير لما الخطوات المطلوبة تتكمّل.

**نتايج التدفق ده على الكود:**

- التلات اختيارات بيحصلوا **قبل** ما يبقى فيه يوزر، فلازم يعيشوا عبر قفزة تسجيل
  الدخول: `localStorage` هو المخزن، و`/login?next=/assessment` هو الجسر
- `app/login/page.tsx` بيوجّه لـ `/dashboard` في مسارين بس (تسجيل الدخول سطر ٣٥،
  والزائر سطر ٨٩)، و**فرع إنشاء الحساب مبيوجّه لحاجة خالص** — بيكتب رسالة نجاح
  ويقف. التلاتة لازم يبقوا واعيين بوجود اختيار معلّق
- اللاندينج فيه **٣ روابط دخول** غير متسقة أصلاً: النافبار `/login`، الهيرو
  `/login`، والفوتر `/dashboard`. لازم يتوحّدوا تحت قاعدة «الاختيار قبل التسجيل»
- `app/assessment/page.tsx` أول خطوة فيه `step: "subject"` — لازم **يستهلك**
  الاختيار الجاهز ويقفز للكويز، مش يسأل عن المادة تاني

```sql
alter table profiles
  add column if not exists persona       text default 'student',
  add column if not exists student_level text,
  add column if not exists subject       text;
```

الأعمدة على `profiles` — مش على `study_days`، لأنها صفة المستخدم مش صفة الدرس.

⚠️ **التكلفة الحقيقية:** كوبي اللاندينج كله جاي من `t.*` في قاموس الـ i18n
بلغتين. يعني أي نص بيختلف حسب الشخصية = ٣ نسخ × ٢ لغات. ده أكبر جزء من الشغل هنا،
مش الكود.

### ٥) الترتيب — دي تسبق تقسيم الدرس

الشخصية بتغيّر محتوى مرحلة ٥ و ٦ ونبرة البرومبتات. فلو بنيت `StageTimeline`
بعناوين ثابتة الأول، هرجع أعدّلها تاني. **فالعمود والبرومبتات ونصوص المراحل
يتعملوا قبل المرحلة ١**، وده أرخص من إني أرجع أفكّها بعدين.

---

## ثامناً: أثر الشخصية على الداشبورد + حجم الشغل

### ١) اللي فعلاً «للطلاب بس»

| العنصر | حجمه | الحكم |
|--------|------|-------|
| `ParentReportModal` | ٨٥ سطر | **طلبة مدارس بس.** «تقرير ولي الأمر» مالوش أي معنى لفري لانسر |
| `LeaderboardModal` | ٨٥ سطر | طالب/خريج ممكن؛ فري لانسر بيتنافس مع مين؟ |
| غرف الدراسة (`StudyRoom`) | ١٧٨ سطر | مذاكرة جماعية = طالب. للخريج تتحول «مراجعة كود»، وللفري لانسر تتشال |
| `BossFight` | ٤٥٩ سطر | نبرة ألعاب — بتتقل على اللي بيشتغل |
| `ShopModal` | ٨٧ سطر | متجر نقاط — نفس الكلام |
| صفحة `/community` | ٢٦٤ سطر | تلات تابات (متصدرين / تحدي / غرف) — كلها بنبرة الطلبة |

المجموع ≈ **١١٦٠ سطر** بنبرة الطلبة. المطلوب مش حذفها — **إخفاء أو إعادة تسمية
حسب الشخصية**، لأن الطالب لسه محتاجها.

### ٢) خبر حلو يقلل الشغل

`components/StudyPet.tsx` (١٢٨ سطر) و `components/AskClassmatesButton.tsx`
(٥٣ سطر) **مش مستوردين في أي ملف**. كود ميت. ١٨١ سطر يتحذفوا بدل ما يتعاملوا.

### ٣) قد إيه الشغل

الوحدة اللي أقدر أقيس بيها بصدق هي **جلسة شغل** = سلسلة تعديلات أقدر أخلّصها قبل
ما الذاكرة تمتلي وأحتاج أبدأ من أول. تقديري:

| الشغل | جلسات | إيه اللي بيحكم |
|-------|-------|----------------|
| طبقة الشخصية (عمود + توحيد `getUiText` + مختار اللاندينج) | ١ | مباشر |
| كوبي الشخصيات في i18n | ١–٢ | **٩٩ مفتاح لاندينج × ٣ × ٢ لغات** — الكتابة هي التكلفة مش الكود |
| إخفاء/تسمية عناصر الداشبورد حسب الشخصية | ١ | ٦ ملفات، تعديلات صغيرة |
| تقسيم صفحة الدرس + بلوكات الكود + التايم لاين (مرحلة ١) | ٢–٣ | ١٠٤٥ سطر و ٢٦ متغير حالة |
| مرحلة ٢ (شيبس + ملخص + Practice + ملاحظات) | ١–٢ | بتستخدم موجود |
| الـ Playground | ٢–٣ | iframe معزول + مؤقّت + CodeMirror |

يعني **من الشخصية لحد آخر مرحلة ٢ ≈ ٥–٧ جلسات**، والـ Playground لوحده بعدها.

### ٤) أكبر خطر في التقدير — مش الكود

أنا **مش بقدر أبني المشروع ولا أشوفه** في البيئة اللي شغال فيها (الـ node_modules
متسطّبة على ويندوز). اللي بقدر عليه `npx tsc --noEmit` + فحص بالـ grep. يعني كل
جلسة لازم **إنت** تشغّل `npm run dev` وتقوللي شكله إيه. لو مشينا كتير من غير
مراجعة منك، احتمال أرجع أصلّح شغل خلصته بقى أكبر من الشغل نفسه.

**النصيحة:** جلسة واحدة، تشوف النتيجة، بعدها نكمّل. أول جلسة تكون طبقة الشخصية
لوحدها — أصغر واحدة وأوضح فرق.
