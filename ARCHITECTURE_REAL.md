# ARCHITECTURE.md — الحقيقة (بعد Reality Sync)

ملاحظة: كل ما كان مكتوب قبل في ARCHITECTURE.md كان مبني على افتراض "نبني نظام جديد" مع Hermes كمحور وحيد و3 planes نظرية. ده مش متطابق مع الواقع.

## الواقع الفعلي

### المنتج (Product Plane — الحقيقي، مش النظري)
- اسم المشروع في الكود: smart-study-assistant
- Stack: Next.js + Supabase (DB + Auth) + Vercel deploy + RTL + responsive
- موقع: magiclly.com (مفهرس من Google)
- ليس "Hermes هو التطبيق للطالب" — Hermes هو أداة الشركة (CTO + تنفيذ). الطالب يستخدم تطبيق الويب المنفصل.

### مراحل التعليم (6، مش 3 بس)
من الكود الفعلي:
1. Primary — الأقوى والأكمل
2. Preparatory — قوي
3. Secondary — قيد التطوير حاليًا (الأولوية)
4. University — موجود جزئيًا (كلية / قسم / فصل دراسي / كورسات)
5. Graduate — مبكر
6. Freelancer — مبكر

كل مرحلة لها Dashboard خاص بها. مش "نفس المنتج مع صعوبة مختلفة".

### الذكاء الاصطناعي (الحقيقي، مش Tutor OS النظري)
- الموجود: AI Router حقيقي + نماذج متعددة + APIs حقيقية + أجزاء Mock/Placeholder
- مش مطبق: الـ 5 subsystems (Diagnostic / Policy / Sequencing / Memory / Transfer-eval) كوحدة متكاملة
- مش موجود: "فصل كامل بين Tutor OS و CTO bot" كواقع — ده هدف تصميم مستقبلي
- الهدف المستقبلي: تحويل الـ AI من "Router يوجّه لنموذج" لـ "Tutor OS" مع graph + learner memory حقيقي (لكن ده مش موجود دلوقتي)

### البيانات (الحقيقية)
- Supabase DB حقيقية: جداول دروس، خطط دراسة، مراحل، مستخدمين، محتوى، مكافآت، عبادة
- مش موجود: schema نظري للـ Learner Memory / NodeMastery / Evidence كما في DATABASE.md النظري
- الـ Adapter (امتحانات MoE) موجود كمحتوى لكن مفيش "جدار صلب" معزول فعليًا عن التعليم

### الشركة (Company Operating Plane — Hermes فقط)
- Magiclly CTO (أنا) = البوت الاستراتيجي داخل Hermes
- تنفيذ الكود والمراجعة = داخل Hermes
- لا ZCode — متوقف نهائيًا
- كل القرارات تُسجّل في DECISIONS.md وتُراجع قبل أي تنفيذ

### مبادئ معمارية (معدّلة من الواقع)
1. الواقع أولًا — لا نبني على افتراضات نظرية
2. المنتج الحالي هو الأساس — نطوره، مش نستبدله
3. المراحل الـ 6 موجودة — لا نتجاهل University / Graduate / Freelancer
4. AI Router موجود — نطوّره تدريجيًا نحو Tutor OS، مش نعيد بناؤه من الصفر
5. البيانات في Supabase — نستخدمها، مش نتجاهلها لصالح schema نظري
6. Hermes = أداة الشركة فقط — الطالب لا يستخدم Hermes
7. الكود في smart-study-assistant هو المصدر — كل ملف نظري يُراجع ضده
8. لا ميزات جديدة قبل ما نصلح المعرفة أولًا (P0)

### الفجوة بين الرؤية والواقع
الرؤية الكبيرة (VISION.md): نظام تعليمي عميق شخصي بـ 3 مراحل — صحيحة كاتجاه، لكن:
- الواقع فيه 6 مراحل مش 3
- الواقع فيه AI Router مش Tutor OS كامل
- الواقع فيه مستخدمين مبكرين مش "ننتظر بناء"
- الرؤية تحتاج تُعدّل لتشمل المراحل الـ 6 والـ AI الحالي كمنطلق

### ما هو مفتوح / غير مقفل
- كيف نطوّر Secondary (الأولوية الفعلية حاليًا)
- كيف نربط الـ AI Router بـ Tutor OS تدريجيًا
- كيف نعيد كتابة EDUCATION_SYSTEM و AI_SYSTEM و DATABASE حسب الواقع
- هل نبدأ v0 Math Graph (متوقف مؤقتًا) قبل ما نكمل Secondary — لا، نكمل Secondary أولًا حسب الواقع
