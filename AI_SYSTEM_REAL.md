# AI_SYSTEM.md — الحقيقة (بعد Reality Sync)

تحذير: الملف النظري السابق افترض "Tutor OS" مع 5 subsystems متكاملة (Diagnostic / Policy / Sequencing / Memory / Transfer-eval) كوحدة واحدة، مع "LLM = mouth/reasoner" و"Exam adapter معزول بجدار صلب". ده مش متطابق مع الكود.

## الواقع الفعلي (من smart-study-assistant / الكود)

### ما هو موجود فعليًا
- AI Router حقيقي: يوجّه الطلب لنموذج مناسب حسب السياق.
- نماذج متعددة متصلة (من ملفات .env و scripts و config): مش نموذج واحد — ده حقيقي.
- APIs حقيقية للذكاء الاصطناعي (مش Mock بالكامل — لكن أجزاء كتير من التفاعل قد تكون Placeholder أو جزئية).
- تفاعل AI حسب المرحلة: كل مرحلة (Primary / Preparatory / Secondary / ...) لها سياق تعليمي مختلف في الـ Dashboard — لكن مش مؤكد إن الـ AI "يفهم" المرحلة بشكل ذكي أو مجرد "يعرض المحتوى المناسب".
- محتوى AI Chat موجود في الـ UI — لكن جودة الاستجابة / مدى ارتباطها بـ concept graph / learner memory مش مؤكد من الكود فقط.

### ما هو غير موجود (أو غير مؤكد)
- "5 subsystems" كوحدة متكاملة: UNKNOWN إذا كانت موجودة ككود حقيقي أو مجرد تصميم نظري. من الكود المتاح، لا يوجد دليل واضح على وحدة متكاملة بهذه الأسماء.
- "Diagnostic model" ككيان منفصل: UNKNOWN في الكود — قد يكون جزء من الـ Router أو غير موجود كوحدة.
- "Learner Memory" منظم كـ NodeMastery + Evidence: مش موجود في DB الحقيقية بالشكل النظري. DB الحقيقية فيها مستخدمين + دروس + خطط — مش "memory per concept node".
- "Transfer evaluation" كبروتوكول مستقل: UNKNOWN — قد يكون جزء من نظام الأسئلة أو غير موجود.
- "Teaching policy" كإطار منظم: UNKNOWN — موجود محتوى تعليمي لكن مش مؤكد إن فيه "policy engine" منفصل.
- "Exam adapter معزول بجدار صلب": مش مؤكد من الكود. محتوى الامتحان موجود لكن مش معزول رسميًا.
- "LLM = mouth/reasoner تحت graph + policy": افتراض نظري — الواقع هو Router يوجّه لنموذج مع بعض السياق، مش "نموذج مقيد بـ graph حقيقي".

### الفجوة بين الرؤية والواقع
الرؤية (من AI_SYSTEM.md النظري و VISION.md): نظام Tutor ذكي يتعلم الطالب ويكيّف التعليم حسب الفهم الحقيقي.

الواقع: AI Router يقدّم محتوى وتفاعل حسب السياق (مرحلة + مادة) مع نماذج متعددة. ده خطوة أولى جيدة — لكن مش "Tutor OS" بعد.

### ما يجب أن نعمله (من P0 — تصحيح المعرفة أولًا)
قبل أي تطوير لـ AI:
1. نراجع ملفات الـ AI Router (scripts مثل diag-live-routing.mjs، test-ai-router.mjs) ونفهم كيف يعمل فعليًا.
2. نحدد إيه Mock وإيه حقيقي في التفاعل مع الطالب.
3. نقرر إذا كنا نطوّر الـ Router تدريجيًا نحو Tutor OS، أو نعيد تصميمه من الأساس — لكن القرار لازم يكون مبني على فهم الواقع مش على افتراض.

### قيود CTO
- لا نعيد بناء AI من الصفر بناءً على AI_SYSTEM.md النظري.
- لا نفترض "5 subsystems" موجودة قبل ما نتحقق من الكود.
- أي اقتراح لـ "Tutor OS" يجب أن يبدأ من الـ Router الموجود ويطوّره تدريجيًا.
- Secondary هي الأولوية — أي تطوير AI يجب أن يخدم Secondary أولًا (ليس Graduate أو Freelancer).
- Hermes فقط للتنفيذ — لا نعيد إدخال أي نظام تنفيذ موازي.

### UNKNOWN (غير مؤكد — لا نفترض)
- كيفية عمل الـ Router بالتفصيل (من الكود المتاح فقط).
- أي النماذج متصلة فعليًا في الإنتاج.
- هل فيه "learning memory" حقيقية أم مجرد جلسات مستقلة.
- مدى ارتباط الـ AI بـ DB (هل يقرأ من Supabase مباشرة أم عبر API وسيط).
- هل Secondary له AI مختلف عن Primary من حيث المنطق.
